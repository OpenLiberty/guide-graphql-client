<server description="Query Service">

  <featureManager>
    <feature>jaxrs-2.1</feature>
    <feature>cdi-2.0</feature>
    <feature>jsonb-1.0</feature>
    <feature>mpConfig-2.0</feature>
    <feature>mpOpenAPI-2.0</feature>
  </featureManager>

  <variable name="default.http.port" defaultValue="9084"/>
  <variable name="default.https.port" defaultValue="9447"/>
  <!-- tag::url[] -->
  <variable name="graphql.server" defaultValue="http://localhost:9082/graphql"/>
  <!-- end::url[] -->
  
  <httpEndpoint host="*" httpPort="${default.http.port}"
      httpsPort="${default.https.port}" id="defaultHttpEndpoint"/>

  <webApplication location="client.war" contextRoot="/"/>
</server>
